# shared/

Global constants and shared type definitions.

## Files

| File | Description |
|------|-------------|
| `const.ts` | Global constants: `UI_BASE_PATH` (`"/u"`), `SIDEBAR_MODE_KEY`, `REST_CONSOLE_TABS_KEY`, `HTTP_STATUS_CODES` map |
| `types.ts` | Shared TypeScript types (e.g., `SidebarMode`) |
